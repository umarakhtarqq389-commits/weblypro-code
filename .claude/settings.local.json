{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(until [ -s /tmp/missing_urls.txt ] || ! pgrep -f \"curl -s -o /dev/null\" > /dev/null; do sleep 2; done; echo \"done checking\"; wc -l /tmp/missing_urls.txt)",
      "Bash(xargs -P 10 '-I{}' sh -c ' *)"
    ]
  }
}
